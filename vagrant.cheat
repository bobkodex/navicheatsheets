# vagrant init
vagrant init

# vagrant add virtual box image ubuntu
vagrant box add ubuntu/xenial64

# vagrant vm operaiotns
vagrant up
vagrant ssh
vagrant halt
vagrant destroy

# vagrant generate ssh configuration
vagrant ssh-config